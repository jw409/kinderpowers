---
phase: 01-sequential-thinking-spawn-hints
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/metathinking/SKILL.md
autonomous: true
requirements:
  - SPAWN-03

must_haves:
  truths:
    - "The metathinking skill documents spawn_strategy parameter with four values: none, convergent, divergent, hierarchical"
    - "Each spawn_strategy value has a description explaining when to use it"
    - "The skill documents how spawn_candidate hints from the server relate to spawn_strategy"
  artifacts:
    - path: "skills/metathinking/SKILL.md"
      provides: "spawn_strategy parameter in Parameters table, Spawn Strategy section with routing guidance"
      contains: "spawn_strategy"
  key_links:
    - from: "skills/metathinking/SKILL.md"
      to: "spawn_candidate hint kind"
      via: "Documentation linking server hint to caller strategy"
      pattern: "spawn_candidate.*spawn_strategy"
---

<objective>
Add spawn_strategy parameter to the metathinking skill. This parameter lets callers control how the skill responds to spawn_candidate hints from the sequential thinking server: ignore them (none), spawn agents that must converge on an answer (convergent), spawn agents exploring different approaches (divergent), or spawn layered agents where parent synthesizes children (hierarchical).

Purpose: Complete the "server hints, caller decides" loop by giving callers an explicit knob for spawn behavior.
Output: Updated SKILL.md with spawn_strategy parameter and routing documentation.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current metathinking SKILL.md parameter table format -->
From skills/metathinking/SKILL.md:
```markdown
## Parameters (caller controls)

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `min_thoughts` | 6 | 3-20 | Minimum thought chain length for complex problems |
| `branch_style` | liberal | conservative, liberal, exhaustive | How aggressively to branch |
| `explore_width` | 4 | 2-7 | Default explore_count when widening |
| `self_checks` | true | true/false | Run the four self-checks at layer 1 |
| `search_wiring` | true | true/false | Auto-execute search when incorporate_search is set |
```

<!-- spawn_candidate hint from Plan 01 -->
The server will surface a `spawn_candidate` hint with `spawnMeta`:
- `branchPoints`: array of branch point names
- `recommendedDepth`: suggested thinking depth for subagents
- `recommendedModel`: "same", "cheaper", or "thinking"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add spawn_strategy parameter and Spawn Strategy section to SKILL.md</name>
  <files>skills/metathinking/SKILL.md</files>
  <read_first>
    - skills/metathinking/SKILL.md
  </read_first>
  <action>
1. Add `spawn_strategy` row to the Parameters table (after the `search_wiring` row):

```
| `spawn_strategy` | none | none, convergent, divergent, hierarchical | How to handle spawn_candidate hints from the server |
```

2. Add a new section "## Spawn Strategy" AFTER the "## Server Hints" section (around line 190) and BEFORE the "## Anti-Patterns" section. Content:

```markdown
## Spawn Strategy

When the server surfaces a `spawn_candidate` hint, the `spawn_strategy` parameter controls the response:

### none (default)

Ignore spawn_candidate hints. All exploration happens within the current thinking session. Use when:
- Single-agent work with no orchestrator
- Simple problems that don't warrant parallelism
- Context budget is tight

### convergent

Spawn subagents that explore branch points independently, then merge results. Each subagent gets the same goal but a different starting branch. The parent waits for all subagents, then uses `continuation_mode: "merge"` to synthesize. Use when:
- You need agreement/consensus across approaches
- The problem has a single correct answer explored from multiple angles
- The `convergenceSignal` in the merge summary matters to the caller

Pattern:
```
1. Server hints spawn_candidate with N branch_points
2. Spawn N subagents, each exploring one branch_point
3. Each subagent runs sequential_thinking with recommended_depth thoughts
4. Parent merges: continuation_mode="merge", merge_branches=[all branch IDs]
5. Check mergeSummary.convergenceSignal: "converged" = high confidence answer
```

### divergent

Spawn subagents that explore branch points independently WITHOUT merging. Each subagent produces an independent result. The orchestrator selects the best. Use when:
- You want the widest possible solution space
- Multiple valid answers exist (creative tasks, brainstorming)
- Pruning happens after exploration, not during

Pattern:
```
1. Server hints spawn_candidate with N branch_points
2. Spawn N subagents, each exploring one branch_point
3. Each subagent runs to done_reason independently
4. Orchestrator reviews all results, selects or combines
5. No merge step needed
```

### hierarchical

Spawn subagents in layers. Layer 1 subagents explore, report to a layer 2 synthesizer, which may spawn its own subagents. Use when:
- Deep, multi-level problems (architecture, system design)
- Delegation to specialized subagents at different layers
- The `recommendedModel` in spawn_meta varies by layer depth

Pattern:
```
1. Server hints spawn_candidate at layer 1
2. Spawn subagents for each branch_point with delegate_to_next_layer=true
3. Layer 2 subagent may itself trigger spawn_candidate
4. Results flow up: layer 3 -> layer 2 -> layer 1 merge
5. Parent uses layer-aware merge
```

### Connecting to Server Hints

The `spawn_candidate` hint includes `spawnMeta`:

| Field | Type | Description |
|-------|------|-------------|
| `branchPoints` | string[] | Branch IDs or proposal names to explore |
| `recommendedDepth` | number | Suggested thought count for subagents (3-10) |
| `recommendedModel` | string | "same", "cheaper", or "thinking" |

When `spawn_strategy` is not `none`, use these fields to configure subagent spawning:
- `branchPoints` -> one subagent per point (or batch if too many)
- `recommendedDepth` -> set as subagent's `total_thoughts`
- `recommendedModel` -> map to agent model selection ("thinking" = opus, "cheaper" = sonnet, "same" = current)

The merge summary now includes `branchOutcomes` (per-branch finalConfidence and doneReason) and `convergenceSignal` to help assess results.
```

3. Add a new anti-pattern bullet to the existing "## Anti-Patterns" section:

```
- **Spawning without strategy** -- if you spawn subagents, set spawn_strategy explicitly. Default "none" means hints are informational only.
```

4. Verify no compulsion language was introduced (no "MUST", "NEVER", "not negotiable" etc. in new content).
  </action>
  <verify>
    <automated>grep -c "spawn_strategy" skills/metathinking/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "spawn_strategy" skills/metathinking/SKILL.md` finds parameter table row AND section content
    - `grep -c "spawn_strategy" skills/metathinking/SKILL.md` returns >= 8 (table + section header + 4 strategy names + connecting text)
    - `grep "convergent\|divergent\|hierarchical" skills/metathinking/SKILL.md` finds all three non-none strategies
    - `grep "spawn_candidate" skills/metathinking/SKILL.md` finds references linking server hint to strategy
    - `grep "branchOutcomes\|convergenceSignal" skills/metathinking/SKILL.md` finds references to enhanced merge
    - No compulsion language: `grep -c "MUST\|NEVER" skills/metathinking/SKILL.md` returns 0
  </acceptance_criteria>
  <done>spawn_strategy parameter added to metathinking skill with four values (none/convergent/divergent/hierarchical), each with usage guidance and patterns. Server hint connection documented. No compulsion language.</done>
</task>

</tasks>

<verification>
- `grep "spawn_strategy" skills/metathinking/SKILL.md` finds multiple references
- `grep "convergent.*divergent.*hierarchical" skills/metathinking/SKILL.md` or individual greps find all strategies
- No compulsion language in the file
</verification>

<success_criteria>
- spawn_strategy parameter in Parameters table with default "none" and four values
- Each strategy has a "Use when" section and a "Pattern" section
- Server hint connection documented (spawnMeta fields table)
- Enhanced merge summary (branchOutcomes, convergenceSignal) referenced
- Anti-pattern added for spawning without strategy
- No compulsion language introduced
</success_criteria>

<output>
After completion, create `.planning/phases/01-sequential-thinking-spawn-hints/01-03-SUMMARY.md`
</output>
