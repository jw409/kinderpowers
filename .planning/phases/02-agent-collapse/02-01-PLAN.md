---
phase: 02-agent-collapse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-researcher.md
  - agents/gsd-verifier.md
autonomous: true
requirements:
  - COLLAPSE-01
  - COLLAPSE-02

must_haves:
  truths:
    - "gsd-researcher.md exists with mode parameter accepting phase/project/synthesize"
    - "gsd-verifier.md exists with mode parameter accepting goal-backward/integration/plan-quality/coverage"
    - "Each mode section contains the full behavioral content from its source agent"
  artifacts:
    - path: "agents/gsd-researcher.md"
      provides: "Parameterized researcher agent replacing 3 agents"
      contains: "mode"
    - path: "agents/gsd-verifier.md"
      provides: "Parameterized verifier agent replacing 4 agents"
      contains: "mode"
  key_links:
    - from: "agents/gsd-researcher.md"
      to: "agents/gsd-phase-researcher.md"
      via: "mode=phase contains phase-researcher behavior"
      pattern: "mode.*phase"
    - from: "agents/gsd-verifier.md"
      to: "agents/gsd-integration-checker.md"
      via: "mode=integration contains integration-checker behavior"
      pattern: "mode.*integration"
---

<objective>
Create two new parameterized agent files: gsd-researcher (replaces gsd-phase-researcher, gsd-project-researcher, gsd-research-synthesizer) and gsd-verifier (replaces gsd-verifier, gsd-integration-checker, gsd-plan-checker, gsd-nyquist-auditor).

Purpose: Collapse 7 single-purpose agents into 2 parameterized agents following the pattern established in multi-perspective-review.md and gsd-codebase-mapper.md.
Output: agents/gsd-researcher.md, agents/gsd-verifier.md (updated)
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<interfaces>
<!-- Parameterization pattern from existing agents -->

From agents/multi-perspective-review.md (lines 12-24):
```markdown
## Parameters (caller controls)

The caller tunes the review via their prompt. Parse these from the task description:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `lens_count` | 5 | 2-7 | Number of independent reviewer agents to spawn |
| `mode` | council | council, troll-test, adversarial, gentle, focused | Review personality |
```

From agents/gsd-codebase-mapper.md (lines 29-41):
```markdown
<parameters>
The caller tunes the mapper via their prompt. Parse these:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `depth` | standard | quick, standard, deep, exhaustive | How deep to explore |
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create parameterized gsd-researcher agent</name>
  <files>agents/gsd-researcher.md</files>
  <read_first>
    - agents/gsd-phase-researcher.md (mode=phase source)
    - agents/gsd-project-researcher.md (mode=project source)
    - agents/gsd-research-synthesizer.md (mode=synthesize source)
    - agents/multi-perspective-review.md (parameterization pattern)
    - agents/gsd-codebase-mapper.md (parameterization pattern)
  </read_first>
  <action>
Create agents/gsd-researcher.md with this structure:

**YAML frontmatter:**
```yaml
---
name: gsd-researcher
description: "Parameterized research agent. Mode controls scope: phase (technical approach for one phase), project (domain ecosystem for roadmap), synthesize (merge parallel research outputs). Replaces gsd-phase-researcher, gsd-project-researcher, gsd-research-synthesizer."
tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*
color: cyan
---
```

**Parameters section** (immediately after role block, following multi-perspective-review pattern):
```markdown
## Parameters (caller controls)

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `mode` | phase | phase, project, synthesize | Research scope |

### mode=phase (default)
Answers "What do I need to know to PLAN this phase well?" Produces RESEARCH.md consumed by gsd-planner.
Spawned by /gsd:plan-phase or /gsd:research-phase.

### mode=project
Answers "What does this domain ecosystem look like?" Writes research files in .planning/research/.
Spawned by /gsd:new-project or /gsd:new-milestone.

### mode=synthesize
Reads outputs from parallel researcher agents and synthesizes into SUMMARY.md.
Spawned by /gsd:new-project after 4 researcher agents complete.
```

**Body structure:** Use a `<mode_behaviors>` section with three subsections. For each mode, copy the FULL behavioral content from the source agent:

- `<mode_phase>` — Copy the complete content from gsd-phase-researcher.md: role details, project_context, upstream_input, downstream_consumer, philosophy, tool_strategy, source_hierarchy, verification_protocol, output_format (RESEARCH.md structure), execution_flow, structured_returns, success_criteria. Strip the `<role>` section (already in shared header) but keep ALL other XML blocks.

- `<mode_project>` — Copy the complete content from gsd-project-researcher.md: role details, philosophy, research_modes, tool_strategy, verification_protocol, output_formats (SUMMARY.md, STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md, COMPARISON.md, FEASIBILITY.md), execution_flow, structured_returns, success_criteria. Strip the `<role>` section.

- `<mode_synthesize>` — Copy the complete content from gsd-research-synthesizer.md: role details, downstream_consumer, execution_flow (read files, synthesize, extract, derive, assess, write, commit, return), output_format, structured_returns, success_criteria. Strip the `<role>` section.

**Shared sections** (above mode_behaviors, applying to ALL modes):
- `<role>` — Combined: "You are a GSD researcher. Your mode parameter controls scope..."
- `<project_context>` — Copy from gsd-phase-researcher.md (identical across researchers)
- Shared philosophy (training-as-hypothesis, honest reporting, investigation-not-confirmation) — merge from phase and project researchers (they're nearly identical)
- Shared tool_strategy (Context7 > WebFetch > WebSearch priority, verification protocol) — merge from both (nearly identical)

**Critical: Do NOT summarize or abridge the mode content.** Each mode section must contain enough detail that an executor operating in that mode has the same information as the original standalone agent. The parameterized agent IS the source agent when a mode is active.
  </action>
  <verify>
    <automated>grep -c "mode=phase\|mode=project\|mode=synthesize\|mode_phase\|mode_project\|mode_synthesize" agents/gsd-researcher.md | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-researcher.md exists
    - YAML frontmatter has name: gsd-researcher
    - Parameters table lists mode with phase/project/synthesize
    - mode_phase section contains RESEARCH.md output format
    - mode_project section contains SUMMARY.md, STACK.md, FEATURES.md output formats
    - mode_synthesize section contains synthesis execution flow
    - Shared tool_strategy section exists with Context7 priority
  </acceptance_criteria>
  <done>agents/gsd-researcher.md exists with all 3 modes containing full behavioral content from source agents</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild gsd-verifier as parameterized agent with 4 modes</name>
  <files>agents/gsd-verifier.md</files>
  <read_first>
    - agents/gsd-verifier.md (mode=goal-backward source, current file)
    - agents/gsd-integration-checker.md (mode=integration source)
    - agents/gsd-plan-checker.md (mode=plan-quality source)
    - agents/gsd-nyquist-auditor.md (mode=coverage source)
    - agents/multi-perspective-review.md (parameterization pattern)
  </read_first>
  <action>
Rewrite agents/gsd-verifier.md as a parameterized agent with this structure:

**YAML frontmatter:**
```yaml
---
name: gsd-verifier
description: "Parameterized verification agent. Mode controls what is verified: goal-backward (phase goal achievement in code), integration (cross-phase wiring), plan-quality (plan completeness before execution), coverage (Nyquist test coverage). Replaces gsd-verifier, gsd-integration-checker, gsd-plan-checker, gsd-nyquist-auditor."
tools: Read, Write, Bash, Grep, Glob, Edit
color: green
---
```

Note: tools list is the UNION of all 4 source agents' tools. gsd-verifier had Read,Write,Bash,Grep,Glob. gsd-integration-checker had Read,Bash,Grep,Glob. gsd-plan-checker had Read,Bash,Glob,Grep. gsd-nyquist-auditor had Read,Write,Edit,Bash,Glob,Grep.

**Parameters section:**
```markdown
## Parameters (caller controls)

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `mode` | goal-backward | goal-backward, integration, plan-quality, coverage | What to verify |

### mode=goal-backward (default)
Verifies phase goal achievement through goal-backward analysis. Checks codebase delivers what phase promised.
Creates VERIFICATION.md report. Spawned by /gsd:verify-work or /gsd:execute-phase.

### mode=integration
Verifies cross-phase integration and E2E flows. Checks phases connect properly.
Spawned by /gsd:audit-milestone.

### mode=plan-quality
Verifies plans will achieve phase goal before execution. Goal-backward analysis of plan quality.
Spawned by /gsd:plan-phase after planner creates PLAN.md.

### mode=coverage
Fills Nyquist validation gaps by generating tests and verifying coverage.
Spawned by /gsd:validate-phase.
```

**Body structure:** Use `<mode_behaviors>` with four subsections:

- `<mode_goal_backward>` — Copy complete content from current gsd-verifier.md: core_principle, verification_process (Steps 0-10), output format (VERIFICATION.md), critical_rules, stub_detection_patterns, success_criteria.

- `<mode_integration>` — Copy complete content from gsd-integration-checker.md: core_principle, inputs, verification_process (Steps 1-6), output format, critical_rules, success_criteria.

- `<mode_plan_quality>` — Copy complete content from gsd-plan-checker.md: core_principle, upstream_input, verification_dimensions (all 9 dimensions), verification_process (Steps 1-10), examples, issue_structure, structured_returns, anti_patterns, success_criteria.

- `<mode_coverage>` — Copy complete content from gsd-nyquist-auditor.md: execution_flow (load_context, analyze_gaps, generate_tests, run_and_verify, debug_loop, report), structured_returns, success_criteria. Note: implementation files are READ-ONLY in this mode.

**Shared sections:**
- `<role>` — Combined verifier role with mode routing
- `<project_context>` — From gsd-verifier.md

**Critical: Do NOT summarize.** Each mode must contain the full behavioral spec from its source agent.
  </action>
  <verify>
    <automated>grep -c "mode=goal-backward\|mode=integration\|mode=plan-quality\|mode=coverage\|mode_goal_backward\|mode_integration\|mode_plan_quality\|mode_coverage" agents/gsd-verifier.md | xargs test 4 -le</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-verifier.md exists with updated frontmatter
    - Parameters table lists mode with goal-backward/integration/plan-quality/coverage
    - mode_goal_backward section contains VERIFICATION.md output format and Steps 0-10
    - mode_integration section contains export/import map verification
    - mode_plan_quality section contains all 9 verification dimensions
    - mode_coverage section contains test generation loop with max 3 iterations
    - Tools list includes Edit (from nyquist-auditor)
  </acceptance_criteria>
  <done>agents/gsd-verifier.md rebuilt with 4 modes, each containing full behavioral content from source agents</done>
</task>

</tasks>

<verification>
- Both new agent files exist with correct YAML frontmatter
- Each mode in each agent contains substantive content (not stubs)
- Parameters sections follow multi-perspective-review.md / gsd-codebase-mapper.md pattern
- grep for each mode value returns matches in the corresponding agent file
</verification>

<success_criteria>
- agents/gsd-researcher.md has 3 modes with full behavioral content
- agents/gsd-verifier.md has 4 modes with full behavioral content
- Both follow the established parameterization pattern (Parameters table, mode subsections)
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-collapse/02-01-SUMMARY.md`
</output>
