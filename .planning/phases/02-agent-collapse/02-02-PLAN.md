---
phase: 02-agent-collapse
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/gsd-ui.md
  - agents/gsd-planner.md
autonomous: true
requirements:
  - COLLAPSE-03
  - COLLAPSE-04

must_haves:
  truths:
    - "gsd-ui.md exists with mode parameter accepting spec/audit/validate"
    - "gsd-planner.md has scope parameter accepting phase/milestone/project"
    - "Each mode/scope section contains the full behavioral content from its source agent"
  artifacts:
    - path: "agents/gsd-ui.md"
      provides: "Parameterized UI agent replacing 3 agents"
      contains: "mode"
    - path: "agents/gsd-planner.md"
      provides: "Planner with roadmapper absorbed via scope parameter"
      contains: "scope"
  key_links:
    - from: "agents/gsd-ui.md"
      to: "agents/gsd-ui-researcher.md"
      via: "mode=spec contains ui-researcher behavior"
      pattern: "mode.*spec"
    - from: "agents/gsd-planner.md"
      to: "agents/gsd-roadmapper.md"
      via: "scope=project contains roadmapper behavior"
      pattern: "scope.*project"
---

<objective>
Create parameterized gsd-ui agent (replaces gsd-ui-researcher, gsd-ui-auditor, gsd-ui-checker) and add scope parameter to gsd-planner (absorbs gsd-roadmapper).

Purpose: Collapse 4 more single-purpose agents into 2 parameterized agents.
Output: agents/gsd-ui.md (new), agents/gsd-planner.md (updated with scope parameter)
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<interfaces>
<!-- Parameterization pattern from existing agents -->

From agents/multi-perspective-review.md (lines 12-24):
```markdown
## Parameters (caller controls)

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `mode` | council | council, troll-test, adversarial, gentle, focused | Review personality |
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create parameterized gsd-ui agent</name>
  <files>agents/gsd-ui.md</files>
  <read_first>
    - agents/gsd-ui-researcher.md (mode=spec source)
    - agents/gsd-ui-auditor.md (mode=audit source)
    - agents/gsd-ui-checker.md (mode=validate source)
    - agents/multi-perspective-review.md (parameterization pattern)
  </read_first>
  <action>
Create agents/gsd-ui.md with this structure:

**YAML frontmatter:**
```yaml
---
name: gsd-ui
description: "Parameterized UI agent. Mode controls function: spec (produce UI-SPEC.md design contract), audit (retroactive 6-pillar visual audit producing UI-REVIEW.md), validate (verify UI-SPEC.md quality before planning). Replaces gsd-ui-researcher, gsd-ui-auditor, gsd-ui-checker."
tools: Read, Write, Bash, Grep, Glob, WebSearch, WebFetch, mcp__context7__*
color: "#E879F9"
---
```

Note: tools list is the UNION of all 3 source agents. gsd-ui-researcher had Read,Write,Bash,Grep,Glob,WebSearch,WebFetch,mcp__context7__*. gsd-ui-auditor had Read,Write,Bash,Grep,Glob. gsd-ui-checker had Read,Bash,Glob,Grep.

**Parameters section:**
```markdown
## Parameters (caller controls)

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `mode` | spec | spec, audit, validate | UI workflow stage |

### mode=spec (default)
Produces UI-SPEC.md design contract for frontend phases. Reads upstream artifacts, detects design system state, asks only unanswered questions.
Spawned by /gsd:ui-phase orchestrator.

### mode=audit
Retroactive 6-pillar visual audit of implemented frontend code. Produces scored UI-REVIEW.md.
Spawned by /gsd:ui-review orchestrator.

### mode=validate
Validates UI-SPEC.md design contracts against 6 quality dimensions. Produces BLOCK/FLAG/PASS verdicts. Read-only — never modifies UI-SPEC.md.
Spawned by /gsd:ui-phase orchestrator after spec creation.
```

**Body structure:** Use `<mode_behaviors>` with three subsections:

- `<mode_spec>` — Copy complete content from gsd-ui-researcher.md: upstream_input, downstream_consumer, tool_strategy, shadcn_gate, design_contract_questions, output_format (UI-SPEC.md), execution_flow, structured_returns, success_criteria.

- `<mode_audit>` — Copy complete content from gsd-ui-auditor.md: upstream_input, gitignore_gate, screenshot_approach, audit_pillars (all 6 pillars with scoring), registry_audit, output_format (UI-REVIEW.md), execution_flow, structured_returns, success_criteria.

- `<mode_validate>` — Copy complete content from gsd-ui-checker.md: upstream_input, verification_dimensions (all 6 dimensions with BLOCK/FLAG/PASS criteria), verdict_format, structured_returns, success_criteria. Note: this mode is READ-ONLY.

**Shared sections:**
- `<role>` — Combined: "You are a GSD UI specialist. Your mode parameter controls function..."
- `<project_context>` — From gsd-ui-researcher.md (identical pattern)

**Critical: Do NOT summarize.** Each mode must contain the full behavioral spec.
  </action>
  <verify>
    <automated>grep -c "mode=spec\|mode=audit\|mode=validate\|mode_spec\|mode_audit\|mode_validate" agents/gsd-ui.md | xargs test 3 -le</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-ui.md exists
    - YAML frontmatter has name: gsd-ui
    - Parameters table lists mode with spec/audit/validate
    - mode_spec section contains shadcn_gate and design_contract_questions
    - mode_audit section contains 6-pillar scoring and screenshot_approach
    - mode_validate section contains 6 verification_dimensions with BLOCK/FLAG/PASS
  </acceptance_criteria>
  <done>agents/gsd-ui.md exists with 3 modes containing full behavioral content from source agents</done>
</task>

<task type="auto">
  <name>Task 2: Add scope parameter to gsd-planner (absorb roadmapper)</name>
  <files>agents/gsd-planner.md</files>
  <read_first>
    - agents/gsd-planner.md (current file — scope=phase source)
    - agents/gsd-roadmapper.md (scope=project source)
    - agents/multi-perspective-review.md (parameterization pattern)
  </read_first>
  <action>
Update agents/gsd-planner.md to add a scope parameter that absorbs roadmapper functionality.

**YAML frontmatter update:**
```yaml
---
name: gsd-planner
description: "Parameterized planning agent. Scope controls granularity: phase (executable PLAN.md files for one phase), milestone (not yet implemented), project (ROADMAP.md + STATE.md from requirements). Replaces gsd-planner and gsd-roadmapper."
tools: Read, Write, Bash, Glob, Grep, WebFetch, mcp__context7__*
color: green
---
```

**Add Parameters section** immediately after the opening `<role>` block (before `<project_context>`):

```markdown
## Parameters (caller controls)

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `scope` | phase | phase, milestone, project | Planning granularity |

### scope=phase (default)
Creates executable PLAN.md files for a single phase. Decomposes phase into parallel-optimized plans with 2-3 tasks each.
Spawned by /gsd:plan-phase orchestrator.

### scope=milestone
Reserved for future use. Currently routes to scope=phase behavior.

### scope=project
Creates ROADMAP.md + STATE.md from requirements. Maps requirements to phases, derives success criteria, validates coverage.
Spawned by /gsd:new-project orchestrator. Replaces gsd-roadmapper.
```

**Move existing planner content into `<scope_phase>` block.** All current content (context_fidelity, philosophy, discovery_levels, task_breakdown, dependency_graph, scope_estimation, plan_format, goal_backward, checkpoints, tdd_integration, gap_closure_mode, revision_mode, execution_flow, structured_returns, success_criteria) becomes the phase scope behavior.

**Add `<scope_project>` block** containing the FULL content from gsd-roadmapper.md:
- downstream_consumer
- philosophy (anti-enterprise, requirements-drive-structure, goal-backward-at-phase-level, coverage-non-negotiable)
- goal_backward_phases (deriving phase success criteria)
- phase_identification (grouping, dependencies, delivery boundaries, numbering, granularity)
- coverage_validation (100% requirement mapping, traceability)
- output_formats (ROADMAP.md structure with TWO representations, STATE.md structure, draft format)
- execution_flow (receive context, extract requirements, load research, identify phases, derive criteria, validate coverage, write files, return, handle revision)
- structured_returns (created, revised, blocked)
- anti_patterns
- success_criteria

**Shared sections remain above scope blocks:**
- `<role>` — Update to mention scope parameter
- `<project_context>` — Keep as-is (applies to all scopes)

**Critical: Do NOT remove any existing planner content.** Wrap it in scope_phase, add scope_project alongside. The file will be large — that's expected. It replaces two agents.
  </action>
  <verify>
    <automated>grep -c "scope=phase\|scope=project\|scope_phase\|scope_project" agents/gsd-planner.md | xargs test 2 -le</automated>
  </verify>
  <acceptance_criteria>
    - agents/gsd-planner.md has updated description mentioning scope parameter
    - Parameters table lists scope with phase/milestone/project
    - scope_phase block contains all existing planner content (plan_format, task_breakdown, etc.)
    - scope_project block contains roadmapper content (coverage_validation, output_formats, etc.)
    - ROADMAP.md structure documentation present in scope_project
    - STATE.md initialization present in scope_project
  </acceptance_criteria>
  <done>agents/gsd-planner.md has scope parameter with project scope containing full roadmapper behavioral content</done>
</task>

</tasks>

<verification>
- Both agent files exist with correct YAML frontmatter
- Each mode/scope contains substantive content (not stubs)
- Parameters sections follow established pattern
- grep for each parameter value returns matches
</verification>

<success_criteria>
- agents/gsd-ui.md has 3 modes with full behavioral content
- agents/gsd-planner.md has scope parameter with phase and project scopes
- Both follow the established parameterization pattern
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-collapse/02-02-SUMMARY.md`
</output>
