---
phase: 02-agent-collapse
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - agents/gsd-phase-researcher.md
  - agents/gsd-project-researcher.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-integration-checker.md
  - agents/gsd-plan-checker.md
  - agents/gsd-nyquist-auditor.md
  - agents/gsd-ui-researcher.md
  - agents/gsd-ui-auditor.md
  - agents/gsd-ui-checker.md
  - agents/gsd-roadmapper.md
autonomous: true
requirements:
  - COLLAPSE-05

must_haves:
  truths:
    - "Old agent names still resolve via thin alias files"
    - "Each alias file sets the correct mode/scope default and delegates to the new agent"
    - "All 10 old agent files are replaced with alias stubs"
  artifacts:
    - path: "agents/gsd-phase-researcher.md"
      provides: "Alias to gsd-researcher mode=phase"
      contains: "gsd-researcher"
    - path: "agents/gsd-project-researcher.md"
      provides: "Alias to gsd-researcher mode=project"
      contains: "gsd-researcher"
    - path: "agents/gsd-research-synthesizer.md"
      provides: "Alias to gsd-researcher mode=synthesize"
      contains: "gsd-researcher"
    - path: "agents/gsd-integration-checker.md"
      provides: "Alias to gsd-verifier mode=integration"
      contains: "gsd-verifier"
    - path: "agents/gsd-plan-checker.md"
      provides: "Alias to gsd-verifier mode=plan-quality"
      contains: "gsd-verifier"
    - path: "agents/gsd-nyquist-auditor.md"
      provides: "Alias to gsd-verifier mode=coverage"
      contains: "gsd-verifier"
    - path: "agents/gsd-ui-researcher.md"
      provides: "Alias to gsd-ui mode=spec"
      contains: "gsd-ui"
    - path: "agents/gsd-ui-auditor.md"
      provides: "Alias to gsd-ui mode=audit"
      contains: "gsd-ui"
    - path: "agents/gsd-ui-checker.md"
      provides: "Alias to gsd-ui mode=validate"
      contains: "gsd-ui"
    - path: "agents/gsd-roadmapper.md"
      provides: "Alias to gsd-planner scope=project"
      contains: "gsd-planner"
  key_links:
    - from: "agents/gsd-phase-researcher.md"
      to: "agents/gsd-researcher.md"
      via: "alias delegation"
      pattern: "gsd-researcher.*mode=phase"
    - from: "agents/gsd-roadmapper.md"
      to: "agents/gsd-planner.md"
      via: "alias delegation"
      pattern: "gsd-planner.*scope=project"
---

<objective>
Replace all 10 old agent files with thin alias stubs that set defaults and delegate to the new parameterized agents.

Purpose: Backward compatibility -- existing workflow files and user muscle memory still work with old agent names until workflows are updated (Plan 04).
Output: 10 alias files in agents/
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 10 thin alias files for old agent names</name>
  <files>
    agents/gsd-phase-researcher.md,
    agents/gsd-project-researcher.md,
    agents/gsd-research-synthesizer.md,
    agents/gsd-integration-checker.md,
    agents/gsd-plan-checker.md,
    agents/gsd-nyquist-auditor.md,
    agents/gsd-ui-researcher.md,
    agents/gsd-ui-auditor.md,
    agents/gsd-ui-checker.md,
    agents/gsd-roadmapper.md
  </files>
  <read_first>
    - agents/gsd-researcher.md (new parameterized agent — verify it exists from Plan 01)
    - agents/gsd-verifier.md (new parameterized agent — verify it exists from Plan 01)
    - agents/gsd-ui.md (new parameterized agent — verify it exists from Plan 02)
    - agents/gsd-planner.md (updated with scope — verify it exists from Plan 02)
  </read_first>
  <action>
Replace each old agent file with a thin alias stub. Every alias follows this exact template:

```markdown
---
name: {old-agent-name}
description: "Alias for {new-agent-name} with {param}={value}. See agents/{new-agent-name}.md for full documentation."
tools: {same tools as the new parameterized agent}
color: {same color as old agent}
alias_for: {new-agent-name}
default_{param}: {value}
---

# {old-agent-name} (alias)

This agent has been consolidated into **{new-agent-name}** with `{param}={value}`.

See: `agents/{new-agent-name}.md`

When spawned as `{old-agent-name}`, behavior is identical to `{new-agent-name}` with `{param}={value}`.

All behavioral content, execution flows, and output formats are defined in the parameterized agent file.
```

**Specific mappings (apply template above for each):**

| Old Agent | New Agent | Parameter | Value |
|-----------|-----------|-----------|-------|
| gsd-phase-researcher | gsd-researcher | mode | phase |
| gsd-project-researcher | gsd-researcher | mode | project |
| gsd-research-synthesizer | gsd-researcher | mode | synthesize |
| gsd-integration-checker | gsd-verifier | mode | integration |
| gsd-plan-checker | gsd-verifier | mode | plan-quality |
| gsd-nyquist-auditor | gsd-verifier | mode | coverage |
| gsd-ui-researcher | gsd-ui | mode | spec |
| gsd-ui-auditor | gsd-ui | mode | audit |
| gsd-ui-checker | gsd-ui | mode | validate |
| gsd-roadmapper | gsd-planner | scope | project |

**Preserve original colors:**
- gsd-phase-researcher: cyan
- gsd-project-researcher: cyan
- gsd-research-synthesizer: purple
- gsd-integration-checker: blue
- gsd-plan-checker: green
- gsd-nyquist-auditor: "#8B5CF6"
- gsd-ui-researcher: "#E879F9"
- gsd-ui-auditor: "#F472B6"
- gsd-ui-checker: "#22D3EE"
- gsd-roadmapper: purple

**Critical:** Each alias must have `alias_for` and `default_{param}` in YAML frontmatter so downstream tooling can resolve aliases programmatically.
  </action>
  <verify>
    <automated>for f in agents/gsd-phase-researcher.md agents/gsd-project-researcher.md agents/gsd-research-synthesizer.md agents/gsd-integration-checker.md agents/gsd-plan-checker.md agents/gsd-nyquist-auditor.md agents/gsd-ui-researcher.md agents/gsd-ui-auditor.md agents/gsd-ui-checker.md agents/gsd-roadmapper.md; do grep -q "alias_for:" "$f" || echo "MISSING alias_for in $f"; done</automated>
  </verify>
  <acceptance_criteria>
    - All 10 old agent files replaced with alias stubs
    - Each alias has alias_for in YAML frontmatter
    - Each alias has default_mode or default_scope in YAML frontmatter
    - Each alias references the correct new agent name
    - Each alias sets the correct default parameter value
    - Each alias preserves the original color
    - No alias contains the full behavioral content (that's in the new agent)
    - Each alias file is under 20 lines of content (thin stub)
  </acceptance_criteria>
  <done>All 10 old agent files are thin aliases pointing to parameterized agents with correct defaults</done>
</task>

</tasks>

<verification>
- All 10 alias files contain alias_for in frontmatter
- Each alias references correct new agent and parameter value
- No alias file exceeds 25 lines total
- grep for "alias_for" across all old agent files returns 10 matches
</verification>

<success_criteria>
- 10 old agent files replaced with thin alias stubs
- Each stub sets the correct mode/scope default
- Backward compatibility maintained (old names still work)
</success_criteria>

<output>
After completion, create `.planning/phases/02-agent-collapse/02-03-SUMMARY.md`
</output>
