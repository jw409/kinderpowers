---
phase: 03-beads-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - gsd/workflows/new-project.md
  - gsd/workflows/plan-phase.md
  - gsd/workflows/execute-phase.md
  - gsd/workflows/verify-phase.md
  - gsd/workflows/ship.md
autonomous: true
requirements:
  - BEADS-01
  - BEADS-02
  - BEADS-03
  - BEADS-04
  - BEADS-05

must_haves:
  truths:
    - "new-project.md creates an epic bead and stores its ID in .planning/config.json"
    - "plan-phase.md creates a task bead as child of the epic and stores its ID in STATE.md"
    - "execute-phase.md marks the phase bead as in_progress"
    - "verify-phase.md attaches verification evidence to the phase bead notes"
    - "ship.md closes the phase bead with PR link in notes"
    - "All bead calls are wrapped in beadsAvailable checks — zero errors when bd missing"
  artifacts:
    - path: "gsd/workflows/new-project.md"
      provides: "Epic bead creation at project init"
      contains: "bead create"
    - path: "gsd/workflows/plan-phase.md"
      provides: "Task bead creation at phase planning"
      contains: "bead create"
    - path: "gsd/workflows/execute-phase.md"
      provides: "Bead status update to in_progress"
      contains: "bead update"
    - path: "gsd/workflows/verify-phase.md"
      provides: "Verification evidence attached to bead"
      contains: "bead update"
    - path: "gsd/workflows/ship.md"
      provides: "Bead close with PR link"
      contains: "bead close"
  key_links:
    - from: "gsd/workflows/new-project.md"
      to: "gsd/bin/gsd-tools.cjs"
      via: "gsd-tools.cjs bead create"
      pattern: "gsd-tools\\.cjs.*bead create"
    - from: "gsd/workflows/ship.md"
      to: "gsd/bin/gsd-tools.cjs"
      via: "gsd-tools.cjs bead close"
      pattern: "gsd-tools\\.cjs.*bead close"
---

<objective>
Wire bead lifecycle calls into all 5 GSD workflow files so that project init creates an epic bead, phase planning creates child task beads, execution marks them in_progress, verification attaches evidence, and shipping closes them with PR links.

Purpose: Cross-session project memory that survives context compaction — every GSD lifecycle event produces a bead trail.
Output: 5 modified workflow .md files with embedded bash blocks calling gsd-tools bead subcommands.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-beads-integration/03-01-PLAN.md

<interfaces>
<!-- From Plan 01 — the bead CLI commands available to workflows -->
gsd-tools.cjs bead available --raw     → "true" or "false"
gsd-tools.cjs bead create "title" --type epic|task --parent ID --tags a,b --raw  → bead_id or ""
gsd-tools.cjs bead update ID --status in_progress|blocked|done --notes "text" --raw  → output or ""
gsd-tools.cjs bead close ID --notes "text" --raw  → output or ""
gsd-tools.cjs bead show ID --raw       → bead details or ""

Config storage: .planning/config.json field "project_bead" for epic bead ID
State storage: STATE.md field "phase_bead" for current phase bead ID
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire beads into new-project.md and plan-phase.md (create beads)</name>
  <files>gsd/workflows/new-project.md, gsd/workflows/plan-phase.md</files>
  <read_first>gsd/workflows/new-project.md, gsd/workflows/plan-phase.md, gsd/bin/gsd-tools.cjs</read_first>
  <action>
**new-project.md — Add epic bead creation after Step 8 (roadmap commit), before Step 9 (Done):**

Insert a new section between the roadmap commit and the completion summary. Add this bash block:

```markdown
## 8.5. Create Project Bead

Create an epic bead to track the entire project lifecycle (silent no-op if beads not installed):

```bash
BEADS_OK=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead available --raw)
if [ "$BEADS_OK" = "true" ]; then
  PROJECT_NAME=$(grep -m1 'name:' .planning/PROJECT.md 2>/dev/null | sed 's/.*name:[[:space:]]*//' || echo "GSD Project")
  BEAD_ID=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead create "${PROJECT_NAME}" --type epic --tags gsd,project --raw)
  if [ -n "$BEAD_ID" ]; then
    node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" config-set project_bead "$BEAD_ID"
    echo "Created project bead: $BEAD_ID"
  fi
fi
`` `
```

The key points:
- Check beadsAvailable first via `bead available --raw`
- Create epic bead with project name as title
- Store bead ID in config.json via config-set
- Silent if beads not installed (the `if` simply doesn't execute)

**plan-phase.md — Add task bead creation after Step 8 (planner spawn), in Step 9 (Handle Planner Return):**

After the planner returns `## PLANNING COMPLETE` and plans are committed, but before the plan checker step, insert:

```markdown
### Create Phase Bead

Create a task bead for this phase as a child of the project epic (silent no-op if beads not installed):

```bash
BEADS_OK=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead available --raw)
if [ "$BEADS_OK" = "true" ]; then
  PARENT_BEAD=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" config-get project_bead --raw 2>/dev/null)
  PHASE_TITLE="Phase ${PHASE}: ${phase_name}"
  BEAD_ARGS="\"${PHASE_TITLE}\" --type task --tags gsd,phase-${PHASE}"
  if [ -n "$PARENT_BEAD" ]; then
    BEAD_ARGS="${BEAD_ARGS} --parent ${PARENT_BEAD}"
  fi
  PHASE_BEAD=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead create ${BEAD_ARGS} --raw)
  if [ -n "$PHASE_BEAD" ]; then
    node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" state update phase_bead "$PHASE_BEAD"
    echo "Created phase bead: $PHASE_BEAD (child of $PARENT_BEAD)"
  fi
fi
`` `
```

The key points:
- Read parent epic bead ID from config.json
- Create task bead with phase name, tagged with phase number
- Link as child of epic bead if parent exists
- Store phase bead ID in STATE.md via state update
- Silent if beads not installed
  </action>
  <acceptance_criteria>
    - new-project.md contains `bead available --raw`
    - new-project.md contains `bead create` with `--type epic`
    - new-project.md contains `config-set project_bead`
    - plan-phase.md contains `bead available --raw`
    - plan-phase.md contains `bead create` with `--type task`
    - plan-phase.md contains `config-get project_bead`
    - plan-phase.md contains `state update phase_bead`
    - plan-phase.md contains `--parent` for child linkage
    - Both files wrap bead calls in `if [ "$BEADS_OK" = "true" ]` guard
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jw/dev/game1/external/kp-sequential-thinking && grep -c "bead available" gsd/workflows/new-project.md gsd/workflows/plan-phase.md && grep -c "bead create" gsd/workflows/new-project.md gsd/workflows/plan-phase.md && echo "WIRE_CREATE_OK"</automated>
  </verify>
  <done>new-project.md creates epic bead and stores ID in config.json; plan-phase.md creates task bead as child of epic and stores ID in STATE.md; both silently skip when beads unavailable</done>
</task>

<task type="auto">
  <name>Task 2: Wire beads into execute-phase.md, verify-phase.md, and ship.md (update/close beads)</name>
  <files>gsd/workflows/execute-phase.md, gsd/workflows/verify-phase.md, gsd/workflows/ship.md</files>
  <read_first>gsd/workflows/execute-phase.md, gsd/workflows/verify-phase.md, gsd/workflows/ship.md, gsd/bin/gsd-tools.cjs</read_first>
  <action>
**execute-phase.md — Add bead status update in the `validate_phase` step, after STATE.md is updated for phase start:**

After the `state begin-phase` call, add:

```markdown
**Update phase bead to in_progress (silent no-op if beads not installed):**

```bash
BEADS_OK=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead available --raw)
if [ "$BEADS_OK" = "true" ]; then
  PHASE_BEAD=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" state get phase_bead 2>/dev/null | tr -d '"[:space:]')
  if [ -n "$PHASE_BEAD" ] && [ "$PHASE_BEAD" != "null" ]; then
    node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead update "$PHASE_BEAD" --status in_progress --notes "Executing ${PLAN_COUNT} plans"
  fi
fi
`` `
```

**verify-phase.md — Add verification evidence to bead in the `create_report` step, after VERIFICATION.md is written:**

After the VERIFICATION.md file is written, add:

```markdown
**Attach verification evidence to phase bead (silent no-op if beads not installed):**

```bash
BEADS_OK=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead available --raw)
if [ "$BEADS_OK" = "true" ]; then
  PHASE_BEAD=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" state get phase_bead 2>/dev/null | tr -d '"[:space:]')
  if [ -n "$PHASE_BEAD" ] && [ "$PHASE_BEAD" != "null" ]; then
    VERIFY_STATUS=$(grep "^status:" "$REPORT_PATH" | cut -d: -f2 | tr -d ' ')
    VERIFY_SCORE=$(grep "^score:" "$REPORT_PATH" | cut -d: -f2 | tr -d ' ')
    node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead update "$PHASE_BEAD" --notes "Verification: ${VERIFY_STATUS} (${VERIFY_SCORE})"
  fi
fi
`` `
```

**ship.md — Add bead close in the `track_shipping` step, after PR is created:**

After the PR is created and STATE.md is updated, add:

```markdown
**Close phase bead with PR link (silent no-op if beads not installed):**

```bash
BEADS_OK=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead available --raw)
if [ "$BEADS_OK" = "true" ]; then
  PHASE_BEAD=$(node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" state get phase_bead 2>/dev/null | tr -d '"[:space:]')
  if [ -n "$PHASE_BEAD" ] && [ "$PHASE_BEAD" != "null" ]; then
    node "${CLAUDE_PLUGIN_ROOT}/gsd/bin/gsd-tools.cjs" bead close "$PHASE_BEAD" --notes "Shipped: PR #${PR_NUMBER} (${PR_URL})"
  fi
fi
`` `
```

The key points for ALL three files:
- Every bead call is gated behind `bead available --raw` check
- Phase bead ID is read from STATE.md via `state get phase_bead`
- The `tr -d '"[:space:]'` strips any JSON quoting from the state get output
- Null/empty checks prevent passing empty IDs to bead commands
- If beads are unavailable or phase_bead is unset, the entire block silently skips
  </action>
  <acceptance_criteria>
    - execute-phase.md contains `bead update` with `--status in_progress`
    - execute-phase.md contains `state get phase_bead`
    - execute-phase.md wraps bead call in `if [ "$BEADS_OK" = "true" ]`
    - verify-phase.md contains `bead update` with `--notes "Verification:`
    - verify-phase.md contains `state get phase_bead`
    - verify-phase.md wraps bead call in `if [ "$BEADS_OK" = "true" ]`
    - ship.md contains `bead close`
    - ship.md contains `--notes "Shipped: PR`
    - ship.md contains `state get phase_bead`
    - ship.md wraps bead call in `if [ "$BEADS_OK" = "true" ]`
    - No workflow file calls bd directly — all go through gsd-tools.cjs
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jw/dev/game1/external/kp-sequential-thinking && grep -c "bead available" gsd/workflows/execute-phase.md gsd/workflows/verify-phase.md gsd/workflows/ship.md && grep -c "bead update\|bead close" gsd/workflows/execute-phase.md gsd/workflows/verify-phase.md gsd/workflows/ship.md && echo "WIRE_UPDATE_OK"</automated>
  </verify>
  <done>execute-phase.md marks bead in_progress; verify-phase.md attaches verification evidence; ship.md closes bead with PR link; all silently skip when beads unavailable</done>
</task>

</tasks>

<verification>
- All 5 workflow files contain `bead available --raw` guard
- new-project.md: `bead create` with `--type epic`, stores in config.json
- plan-phase.md: `bead create` with `--type task --parent`, stores in STATE.md
- execute-phase.md: `bead update` with `--status in_progress`
- verify-phase.md: `bead update` with verification evidence notes
- ship.md: `bead close` with PR link notes
- No workflow calls `bd` directly — all go through `gsd-tools.cjs bead`
- Removing `bd` from PATH causes zero errors in any workflow (graceful degradation)
</verification>

<success_criteria>
- Complete bead lifecycle: create epic -> create task -> in_progress -> evidence -> close
- Every bead operation silently skips when beads CLI unavailable
- Bead IDs stored in config.json (project_bead) and STATE.md (phase_bead)
- Zero changes to existing workflow behavior when beads unavailable
</success_criteria>

<output>
After completion, create `.planning/phases/03-beads-integration/03-02-SUMMARY.md`
</output>
