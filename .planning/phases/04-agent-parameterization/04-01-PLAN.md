---
phase: 04-agent-parameterization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/code-reviewer.md
  - agents/research-extractor.md
autonomous: true
requirements:
  - PARAM-01
  - PARAM-02

must_haves:
  truths:
    - "code-reviewer accepts focus, pedanticness, and scope parameters with sensible defaults"
    - "research-extractor accepts mode, depth, and output parameters with sensible defaults"
    - "Both agents still work identically when no parameters are specified (backward compatible)"
  artifacts:
    - path: "agents/code-reviewer.md"
      provides: "Parameterized code reviewer agent"
      contains: "Parameters (caller controls)"
    - path: "agents/research-extractor.md"
      provides: "Parameterized research extractor agent"
      contains: "Parameters (caller controls)"
  key_links:
    - from: "agents/code-reviewer.md"
      to: "parameter table"
      via: "Parameters section references focus/pedanticness/scope in body"
      pattern: "focus|pedanticness|scope"
    - from: "agents/research-extractor.md"
      to: "parameter table"
      via: "Parameters section references mode/depth/output in body"
      pattern: "mode|depth|output"
---

<objective>
Parameterize code-reviewer and research-extractor agents with caller-controlled parameters.

Purpose: These two agents become tunable canvases -- callers set focus/depth/scope/output via prompt, agents apply sensible defaults when unspecified.
Output: Two updated agent .md files with parameter tables and parameter-aware body sections.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Reference agents already parameterized -- follow this exact pattern -->
From agents/multi-perspective-review.md (lines 11-24):
- "## Parameters (caller controls)" section after role/description
- Table with columns: Parameter | Default | Range | Description
- "If the caller doesn't specify, use defaults" parsing instructions
- Parameters referenced throughout the body with conditional behavior

From agents/gsd-codebase-mapper.md (lines 29-42):
- <parameters> XML block with same table format
- Parameters parsed from caller prompt
- "If the caller says X -> parameter=Y" natural language parsing hints
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize code-reviewer agent</name>
  <files>agents/code-reviewer.md</files>
  <read_first>agents/code-reviewer.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after the opening description paragraph ("You are a Senior Code Reviewer..."). Use the multi-perspective-review.md pattern (table format).

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `focus` | all | all, security, performance, style, logic | Which aspects to prioritize in review |
| `pedanticness` | medium | low, medium, high | How strict -- low=blocking only, medium=material issues, high=everything including nits |
| `scope` | diff | diff, file, module | How much code to examine -- diff=changed lines, file=full files touched, module=entire module tree |

Add parsing instructions: "Parse these from the caller's prompt. If they say 'security review' -> focus=security. If they say 'be thorough' -> pedanticness=high. If they say 'review the whole module' -> scope=module."

Then update the numbered sections (1-6) in the body to reference parameters:
- Section 1 (Plan Alignment): Always runs regardless of focus
- Section 2 (Code Quality): Depth varies by pedanticness (low=skip style nits, high=flag everything)
- Section 3 (Architecture): Only deep-dive when focus=all or focus=logic
- Section 4 (Documentation): Only when focus=all or focus=style
- Section 5 (Issue Identification): Severity thresholds change with pedanticness (low=Critical only, medium=Critical+Important, high=all including Suggestions)
- Section 6 (Communication): Unchanged

Add a "## Scope Behavior" section explaining:
- diff: Only review changed lines and their immediate context
- file: Review full files containing changes
- module: Review the entire module/directory tree containing changes

Do NOT change the frontmatter (name, description, model, tools). Keep backward compatibility -- when no parameters specified, behavior is identical to current.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" agents/code-reviewer.md | grep -q "1" && grep -q "focus" agents/code-reviewer.md && grep -q "pedanticness" agents/code-reviewer.md && grep -q "scope" agents/code-reviewer.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- agents/code-reviewer.md contains "## Parameters (caller controls)" section
- Parameter table has focus (all/security/performance/style/logic), pedanticness (low/medium/high), scope (diff/file/module)
- Body sections reference parameters with conditional behavior
- Default behavior (no params) unchanged from original
  </acceptance_criteria>
  <done>code-reviewer.md has parameter table with focus/pedanticness/scope, body references parameters, defaults preserve current behavior</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize research-extractor agent</name>
  <files>agents/research-extractor.md</files>
  <read_first>agents/research-extractor.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after the opening description paragraph ("You are a Research Extractor agent..."). Use the multi-perspective-review.md pattern.

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `mode` | extraction | extraction, evaluation, integration | Which analysis pipeline to run (maps to existing Mode 1/2/3) |
| `depth` | standard | quick, standard, deep | How thorough -- quick=L0-L1 only, standard=L0-L2, deep=L0-L3 with full analysis |
| `output` | ranked-list | ranked-list, comparison-table, migration-plan | Output format for findings |

Add parsing instructions: "Parse from caller prompt. 'Should I use this?' -> mode=evaluation. 'How do I integrate?' -> mode=integration. 'Quick scan' -> depth=quick. 'Compare options' -> output=comparison-table."

Update the existing "## Routing: Three Modes" section to reference the `mode` parameter explicitly -- the three modes already exist, just wire them to the parameter name.

Update "## Extraction Levels" to reference the `depth` parameter:
- quick: Only L0 + L1 (structure and value prop)
- standard: L0 through L2 (adds capabilities and architecture)
- deep: L0 through L3 (adds UX innovations, killer insights, limitations)

Update "## Output Format" to add output variants:
- ranked-list (default): Current format -- numbered findings with priority
- comparison-table: Side-by-side comparison (useful for evaluation mode)
- migration-plan: Step-by-step integration plan (useful for integration mode)

Do NOT change the frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" agents/research-extractor.md | grep -q "1" && grep -q "mode" agents/research-extractor.md && grep -q "depth" agents/research-extractor.md && grep -q "output.*ranked-list" agents/research-extractor.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- agents/research-extractor.md contains "## Parameters (caller controls)" section
- Parameter table has mode (extraction/evaluation/integration), depth (quick/standard/deep), output (ranked-list/comparison-table/migration-plan)
- Existing Mode 1/2/3 wired to mode parameter
- Extraction Levels wired to depth parameter
- Output Format section has three variants
  </acceptance_criteria>
  <done>research-extractor.md has parameter table with mode/depth/output, existing sections wired to parameters, defaults preserve current behavior</done>
</task>

</tasks>

<verification>
Both agent files contain a "Parameters (caller controls)" table matching the established pattern from multi-perspective-review.md and gsd-codebase-mapper.md. Parameters are referenced in the body. Default behavior is backward compatible.
</verification>

<success_criteria>
- agents/code-reviewer.md has parameter table and parameter-aware sections
- agents/research-extractor.md has parameter table and parameter-aware sections
- grep confirms "Parameters (caller controls)" in both files
- No frontmatter changes in either file
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-parameterization/04-01-SUMMARY.md`
</output>
