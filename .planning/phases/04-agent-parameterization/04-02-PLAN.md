---
phase: 04-agent-parameterization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/team-coordinator.md
  - agents/gsd-debugger.md
autonomous: true
requirements:
  - PARAM-03
  - PARAM-04

must_haves:
  truths:
    - "team-coordinator accepts worker_count, worker_model, isolation, and coordination parameters"
    - "gsd-debugger accepts method, max_hypotheses, checkpoint_frequency, and escalation parameters"
    - "Both agents still work identically when no parameters are specified (backward compatible)"
  artifacts:
    - path: "agents/team-coordinator.md"
      provides: "Parameterized team coordinator agent"
      contains: "Parameters (caller controls)"
    - path: "agents/gsd-debugger.md"
      provides: "Parameterized gsd debugger agent"
      contains: "Parameters (caller controls)"
  key_links:
    - from: "agents/team-coordinator.md"
      to: "parameter table"
      via: "Parameters section references worker_count/worker_model/isolation/coordination in body"
      pattern: "worker_count|worker_model|isolation|coordination"
    - from: "agents/gsd-debugger.md"
      to: "parameter table"
      via: "Parameters section references method/max_hypotheses/checkpoint_frequency/escalation in body"
      pattern: "method|max_hypotheses|checkpoint_frequency|escalation"
---

<objective>
Parameterize team-coordinator and gsd-debugger agents with caller-controlled parameters.

Purpose: These two agents become tunable -- callers control worker count/model, debugging method, escalation behavior via prompt parameters.
Output: Two updated agent .md files with parameter tables and parameter-aware body sections.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Reference agents already parameterized -- follow this exact pattern -->
From agents/multi-perspective-review.md:
- "## Parameters (caller controls)" section after role/description
- Table: Parameter | Default | Range | Description
- Natural language parsing hints for caller prompts
- Parameters referenced throughout body with conditional behavior

From agents/gsd-codebase-mapper.md:
- <parameters> XML block with same table format
- Parameters parsed from caller prompt
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize team-coordinator agent</name>
  <files>agents/team-coordinator.md</files>
  <read_first>agents/team-coordinator.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after the opening paragraph ("You are a Team Coordinator agent..."). Use the multi-perspective-review.md pattern.

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `worker_count` | auto | auto, 2-7 | Number of parallel workers. auto=determine from task decomposition |
| `worker_model` | sonnet | haiku, sonnet, opus | Default model for spawned workers. Can be overridden per-task based on complexity |
| `isolation` | worktree | worktree, branch, none | How workers are isolated. worktree=git worktrees, branch=separate branches, none=shared workspace |
| `coordination` | inject | inject, checkpoint, freeform | How workers receive context. inject=full upfront context, checkpoint=periodic sync points, freeform=workers coordinate independently |

Add parsing hints: "Parse from caller prompt. 'Use 5 workers' -> worker_count=5. 'Use haiku for all' -> worker_model=haiku. 'No isolation needed' -> isolation=none. 'Let them sync periodically' -> coordination=checkpoint."

Update the existing "## Coordination Protocol" sections to reference parameters:
- Section 1 (Decomposition): worker_count determines target split count (auto=analyze task graph, N=split into N units)
- Section 2 (Task Assignment): worker_model sets the default model. Note: "Select appropriate model" guidance becomes the override -- when a specific task is deterministic, use haiku regardless of worker_model default
- Section 3 (Spawning): isolation determines workspace strategy
- Section 4 (Monitoring): coordination determines sync approach (inject=no mid-task sync needed, checkpoint=check in between phases, freeform=monitor for conflicts)
- Section 5 (Integration): Unchanged

Update "## Principles" to add: "Parameters set defaults, not hard constraints. A worker_model=haiku default still allows opus for complex sub-tasks."

Do NOT change the frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" agents/team-coordinator.md | grep -q "1" && grep -q "worker_count" agents/team-coordinator.md && grep -q "worker_model" agents/team-coordinator.md && grep -q "isolation" agents/team-coordinator.md && grep -q "coordination" agents/team-coordinator.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- agents/team-coordinator.md contains "## Parameters (caller controls)" section
- Parameter table has worker_count (auto/2-7), worker_model (haiku/sonnet/opus), isolation (worktree/branch/none), coordination (inject/checkpoint/freeform)
- Coordination Protocol sections reference parameters
- Default behavior unchanged when no params specified
  </acceptance_criteria>
  <done>team-coordinator.md has parameter table with worker_count/worker_model/isolation/coordination, body references parameters, defaults preserve current behavior</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize gsd-debugger agent</name>
  <files>agents/gsd-debugger.md</files>
  <read_first>agents/gsd-debugger.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section inside the <role> block, after the "Core responsibilities" list. Use the multi-perspective-review.md table pattern.

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `method` | scientific | scientific, bisect, printf, hypothesis | Primary investigation strategy. scientific=full hypothesis testing cycle, bisect=binary search through code/history, printf=observability-first logging, hypothesis=jump straight to hypothesis testing |
| `max_hypotheses` | 5 | 3-10 | Maximum hypotheses to track before forcing a decision or escalation |
| `checkpoint_frequency` | per_hypothesis | never, per_hypothesis, per_test | How often to write checkpoint state to debug file. never=only on completion, per_hypothesis=after each hypothesis formed, per_test=after every test |
| `escalation` | manual | manual, auto | What happens when max_hypotheses exhausted without resolution. manual=checkpoint to user, auto=restart with fresh approach |

Add parsing hints: "Parse from caller prompt. 'Use git bisect' -> method=bisect. 'Add lots of logging' -> method=printf. 'Track up to 8 ideas' -> max_hypotheses=8. 'Auto-restart if stuck' -> escalation=auto."

Update existing sections to reference parameters:

In <investigation_techniques>:
- Add a "## Method Selection" subsection that maps method parameter to technique:
  - scientific: Full Phase 0-4 cycle (default, current behavior)
  - bisect: Start with "Binary Search / Divide and Conquer" technique, skip rubber duck / minimal reproduction unless bisect inconclusive
  - printf: Start with "Observability First" -- add logging everywhere before forming hypotheses
  - hypothesis: Skip Phase 1 evidence gathering, jump to Phase 2 hypothesis formation (useful when caller already has strong suspicion)

In <debug_file_protocol>:
- Reference checkpoint_frequency: "Update the file {per checkpoint_frequency setting}. never=only on status transitions, per_hypothesis=after each hypothesis formed/eliminated, per_test=after every experimental test"

In <execution_flow> investigation_loop:
- After Phase 4 Evaluate ELIMINATED: "If eliminated count >= max_hypotheses and no confirmed root cause: If escalation=auto, clear eliminated list and restart from Phase 0 with 'known_dead_ends' context. If escalation=manual, return CHECKPOINT REACHED."

Do NOT change the frontmatter. Keep backward compatibility -- scientific method with per_hypothesis checkpoints is current default behavior.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" agents/gsd-debugger.md | grep -q "1" && grep -q "method" agents/gsd-debugger.md && grep -q "max_hypotheses" agents/gsd-debugger.md && grep -q "checkpoint_frequency" agents/gsd-debugger.md && grep -q "escalation" agents/gsd-debugger.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- agents/gsd-debugger.md contains "## Parameters (caller controls)" section
- Parameter table has method (scientific/bisect/printf/hypothesis), max_hypotheses (3-10), checkpoint_frequency (never/per_hypothesis/per_test), escalation (manual/auto)
- Investigation techniques section references method parameter
- Debug file protocol references checkpoint_frequency
- Investigation loop references max_hypotheses and escalation
- Default behavior (scientific, 5, per_hypothesis, manual) unchanged
  </acceptance_criteria>
  <done>gsd-debugger.md has parameter table with method/max_hypotheses/checkpoint_frequency/escalation, body sections wire to parameters, defaults preserve current behavior</done>
</task>

</tasks>

<verification>
Both agent files contain a "Parameters (caller controls)" table matching the established pattern. Parameters are referenced in body sections. Default behavior is backward compatible.
</verification>

<success_criteria>
- agents/team-coordinator.md has parameter table and parameter-aware sections
- agents/gsd-debugger.md has parameter table and parameter-aware sections
- grep confirms "Parameters (caller controls)" in both files
- No frontmatter changes in either file
</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-parameterization/04-02-SUMMARY.md`
</output>
