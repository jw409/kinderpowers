---
phase: 05-skill-parameterization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/systematic-debugging/SKILL.md
  - skills/brainstorming/SKILL.md
autonomous: true
requirements:
  - PARAM-05
  - PARAM-06

must_haves:
  truths:
    - "systematic-debugging skill accepts depth, hypothesis_count, and reproduce_first parameters"
    - "brainstorming skill accepts breadth, mode, and time_box parameters"
    - "Both skills work identically when no parameters are specified (backward compatible)"
  artifacts:
    - path: "skills/systematic-debugging/SKILL.md"
      provides: "Parameterized systematic debugging skill"
      contains: "Parameters (caller controls)"
    - path: "skills/brainstorming/SKILL.md"
      provides: "Parameterized brainstorming skill"
      contains: "Parameters (caller controls)"
  key_links:
    - from: "skills/systematic-debugging/SKILL.md"
      to: "parameter table"
      via: "Four Phases section adapts based on depth and hypothesis_count"
      pattern: "depth|hypothesis_count|reproduce_first"
    - from: "skills/brainstorming/SKILL.md"
      to: "parameter table"
      via: "Process sections adapt based on breadth, mode, time_box"
      pattern: "breadth|mode|time_box"
---

<objective>
Parameterize systematic-debugging and brainstorming skills with caller-controlled parameters.

Purpose: Skills become tunable canvases -- callers control investigation depth, brainstorming breadth/mode via prompt parameters.
Output: Two updated SKILL.md files with parameter tables and parameter-aware body sections.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Skills use YAML frontmatter, not the XML <parameters> block that agents use.
     Add parameters section in the markdown body after Overview, using same table format as agents. -->
From agents/multi-perspective-review.md:
- Table: Parameter | Default | Range | Description
- Natural language parsing hints
- Parameters referenced throughout body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize systematic-debugging skill</name>
  <files>skills/systematic-debugging/SKILL.md</files>
  <read_first>skills/systematic-debugging/SKILL.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after the "## Overview" section (after "Understand the problem before changing the code.").

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `depth` | standard | quick, standard, exhaustive | Investigation thoroughness. quick=Phase 1+3 only (skip pattern analysis), standard=all 4 phases, exhaustive=all phases with mandatory multi-component evidence gathering |
| `hypothesis_count` | 3 | 2-8 | Number of hypotheses to generate before investigating any. Higher=more thorough, lower=faster |
| `reproduce_first` | true | true, false | Whether to require consistent reproduction before investigating. false=proceed on reported symptoms alone |

Add parsing hints: "Parse from caller prompt. 'Quick debug' -> depth=quick. 'Be thorough' -> depth=exhaustive. 'I can describe it but can't reproduce' -> reproduce_first=false. 'Generate lots of theories' -> hypothesis_count=6."

Update "## The Four Phases" to reference parameters:

Phase 1 (Root Cause Investigation):
- Step 2 (Reproduce Consistently): "When reproduce_first=false, proceed to Step 3 using reported symptoms. Note: this reduces confidence in findings."
- Step 4 (Gather Evidence): "When depth=exhaustive, ALWAYS perform multi-component boundary logging regardless of system complexity."

Phase 2 (Pattern Analysis):
- "When depth=quick, skip this phase entirely -- go from Phase 1 directly to Phase 3."

Phase 3 (Hypothesis and Testing):
- Step 1: "Generate {hypothesis_count} hypotheses before investigating any. Default 3 -- increase for complex systems, decrease for obvious bugs."

Phase 4 (Implementation):
- Step 4 (If Fix Doesn't Work): "If >= {hypothesis_count} fixes tried: question the architecture."

Update "## Quick Reference" table to note parameter effects on each phase.

Do NOT change the YAML frontmatter (name, description). Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" skills/systematic-debugging/SKILL.md | grep -q "1" && grep -q "depth" skills/systematic-debugging/SKILL.md && grep -q "hypothesis_count" skills/systematic-debugging/SKILL.md && grep -q "reproduce_first" skills/systematic-debugging/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- skills/systematic-debugging/SKILL.md contains "## Parameters (caller controls)" section
- Parameter table has depth (quick/standard/exhaustive), hypothesis_count (2-8), reproduce_first (true/false)
- Four Phases sections reference parameters with conditional behavior
- Default behavior (standard, 3, true) preserves current behavior
  </acceptance_criteria>
  <done>systematic-debugging SKILL.md has parameter table with depth/hypothesis_count/reproduce_first, phases reference parameters, defaults unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize brainstorming skill</name>
  <files>skills/brainstorming/SKILL.md</files>
  <read_first>skills/brainstorming/SKILL.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after the "## Overview" section.

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `breadth` | 3 | 2-8 | Number of alternative approaches to propose. Default 3 matches current "2-3 approaches" guidance |
| `mode` | divergent | divergent, convergent, devil-advocate | Thinking style. divergent=explore widely (default brainstorming), convergent=narrow down existing options, devil-advocate=challenge assumptions and find flaws |
| `time_box` | standard | none, quick, standard | Session depth. none=unlimited, quick=minimal questions then propose, standard=full exploration |

Add parsing hints: "Parse from caller prompt. 'Lots of ideas' -> breadth=6. 'Help me narrow down' -> mode=convergent. 'Challenge my assumptions' -> mode=devil-advocate. 'Quick brainstorm' -> time_box=quick."

Update "## The Process" section to reference parameters:

Under "Understanding the idea":
- time_box=quick: "Ask at most 2 clarifying questions, then propose approaches"
- time_box=standard: Current behavior (ask questions until clear)
- time_box=none: "Explore deeply -- no limit on questions or iteration"

Under "Exploring approaches":
- "Propose {breadth} different approaches with trade-offs" (replaces hardcoded "2-3")
- mode=divergent: "Cast the widest net. Include unconventional approaches. Prioritize variety over feasibility."
- mode=convergent: "Start from the user's existing options or constraints. Narrow down, don't expand. Eliminate weak options with evidence."
- mode=devil-advocate: "For each proposed approach, actively argue against it. Surface hidden assumptions, risks, and failure modes. Present the strongest case for NOT doing each option."

Under "Presenting the design":
- Unchanged (always present design regardless of mode)

Update "## Key Principles" to add: "Parameters shape the session, not constrain it. A quick time_box still allows follow-up if the user wants to explore further."

Do NOT change the YAML frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" skills/brainstorming/SKILL.md | grep -q "1" && grep -q "breadth" skills/brainstorming/SKILL.md && grep -q "mode.*divergent" skills/brainstorming/SKILL.md && grep -q "time_box" skills/brainstorming/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- skills/brainstorming/SKILL.md contains "## Parameters (caller controls)" section
- Parameter table has breadth (2-8), mode (divergent/convergent/devil-advocate), time_box (none/quick/standard)
- Process sections reference parameters with conditional behavior
- Default behavior (3, divergent, standard) preserves current behavior
  </acceptance_criteria>
  <done>brainstorming SKILL.md has parameter table with breadth/mode/time_box, process sections reference parameters, defaults unchanged</done>
</task>

</tasks>

<verification>
Both skill files contain a "Parameters (caller controls)" table. Parameters are referenced in body sections. Default behavior is backward compatible.
</verification>

<success_criteria>
- skills/systematic-debugging/SKILL.md has parameter table and parameter-aware sections
- skills/brainstorming/SKILL.md has parameter table and parameter-aware sections
- grep confirms "Parameters (caller controls)" in both files
- No YAML frontmatter changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill-parameterization/05-01-SUMMARY.md`
</output>
