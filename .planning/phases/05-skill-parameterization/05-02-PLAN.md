---
phase: 05-skill-parameterization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/test-driven-development/SKILL.md
  - skills/verification-before-completion/SKILL.md
autonomous: true
requirements:
  - PARAM-07
  - PARAM-08

must_haves:
  truths:
    - "test-driven-development skill accepts strictness, coverage_target, and test_style parameters"
    - "verification-before-completion skill accepts evidence_bar, auto_run, and check_types parameters"
    - "Both skills work identically when no parameters are specified (backward compatible)"
  artifacts:
    - path: "skills/test-driven-development/SKILL.md"
      provides: "Parameterized TDD skill"
      contains: "Parameters (caller controls)"
    - path: "skills/verification-before-completion/SKILL.md"
      provides: "Parameterized verification skill"
      contains: "Parameters (caller controls)"
  key_links:
    - from: "skills/test-driven-development/SKILL.md"
      to: "parameter table"
      via: "Red-Green-Refactor cycle adapts based on strictness and test_style"
      pattern: "strictness|coverage_target|test_style"
    - from: "skills/verification-before-completion/SKILL.md"
      to: "parameter table"
      via: "Gate and checklist sections adapt based on evidence_bar and check_types"
      pattern: "evidence_bar|auto_run|check_types"
---

<objective>
Parameterize test-driven-development and verification-before-completion skills with caller-controlled parameters.

Purpose: TDD and verification skills become tunable -- callers control strictness, coverage targets, evidence requirements via prompt parameters.
Output: Two updated SKILL.md files with parameter tables and parameter-aware body sections.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From agents/multi-perspective-review.md:
- Table: Parameter | Default | Range | Description
- Natural language parsing hints
- Parameters referenced throughout body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize test-driven-development skill</name>
  <files>skills/test-driven-development/SKILL.md</files>
  <read_first>skills/test-driven-development/SKILL.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after "## Overview".

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `strictness` | standard | minimal, standard, strict | How rigidly to follow TDD. minimal=test after if needed, standard=test first (current Iron Principle), strict=no production code without failing test, no exceptions |
| `coverage_target` | none | none, lines, branches, mutations | What coverage metric to enforce. none=no coverage gate, lines=line coverage %, branches=branch coverage %, mutations=mutation testing |
| `test_style` | auto | auto, unit, integration, e2e, property | Preferred test type. auto=choose based on code being tested (current behavior) |

Add parsing hints: "Parse from caller prompt. 'Quick prototype, skip TDD' -> strictness=minimal. 'Full TDD, no shortcuts' -> strictness=strict. 'Aim for branch coverage' -> coverage_target=branches. 'Property-based tests' -> test_style=property."

Update sections to reference parameters:

"## The Iron Principle":
- strictness=minimal: "Production code first is acceptable. Write tests after to verify behavior. The tradeoff: tests may verify implementation, not requirements."
- strictness=standard: Current text (no change)
- strictness=strict: "NO EXCEPTIONS. Every production line requires a pre-existing failing test. Prototypes, generated code, config files -- all get tests first."

"## Red-Green-Refactor":
- Unchanged for standard/strict. For minimal: "When strictness=minimal, the cycle becomes: GREEN (write code) -> TEST (add tests) -> REFACTOR. You lose the RED verification that tests catch real failures."

"## Good Tests":
- test_style=property: Add guidance: "For property-based tests, define invariants rather than specific input/output pairs. Use the testing framework's property-based testing support (e.g., fast-check, hypothesis)."
- test_style=e2e: "For e2e tests, test complete user journeys. Accept longer setup and execution time."

"## Verification Checklist":
- When coverage_target != none: Add "- [ ] Coverage meets {coverage_target} threshold"

Do NOT change YAML frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" skills/test-driven-development/SKILL.md | grep -q "1" && grep -q "strictness" skills/test-driven-development/SKILL.md && grep -q "coverage_target" skills/test-driven-development/SKILL.md && grep -q "test_style" skills/test-driven-development/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- skills/test-driven-development/SKILL.md contains "## Parameters (caller controls)" section
- Parameter table has strictness (minimal/standard/strict), coverage_target (none/lines/branches/mutations), test_style (auto/unit/integration/e2e/property)
- Iron Principle and Red-Green-Refactor sections adapt to strictness
- Verification Checklist adapts to coverage_target
- Default behavior (standard, none, auto) preserves current behavior
  </acceptance_criteria>
  <done>TDD SKILL.md has parameter table with strictness/coverage_target/test_style, body sections reference parameters, defaults unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize verification-before-completion skill</name>
  <files>skills/verification-before-completion/SKILL.md</files>
  <read_first>skills/verification-before-completion/SKILL.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after "## Overview".

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `evidence_bar` | standard | low, standard, high, auditor | How much evidence required before claiming completion. low=single passing test run, standard=test+build (current), high=test+build+lint+types, auditor=all checks + independent reproduction |
| `auto_run` | true | true, false | Whether to automatically run verification commands. false=prompt user to run manually (useful in environments where auto-run is restricted) |
| `check_types` | all | tests, build, lint, types, all | Which verification checks to perform. Comma-separated or 'all'. Controls which items in the Verification Checklist are required |

Add parsing hints: "Parse from caller prompt. 'Just check tests pass' -> check_types=tests, evidence_bar=low. 'Full audit' -> evidence_bar=auditor. 'Don't run commands automatically' -> auto_run=false."

Update sections to reference parameters:

"## The Gate":
- Step 1 (IDENTIFY): "Based on check_types, determine which commands are needed: tests=test runner, build=build command, lint=linter, types=type checker, all=all of the above."
- Step 2 (RUN): "When auto_run=false, present the command and ask the user to run it. Proceed only after user provides output."

"## What Counts as Evidence":
- evidence_bar=low: "A single verification command passing is sufficient."
- evidence_bar=standard: Current table (test command + build command minimum)
- evidence_bar=high: "All checks must pass: tests, build, lint, and type check. Partial passes are not sufficient."
- evidence_bar=auditor: "All checks pass AND independent reproduction: re-run in clean environment, verify from scratch. No caching, no shortcuts."

"## Verification Checklist":
- Wrap each section with its check_type tag:
  - "### Code Changes" items: tests -> test item, build -> build item, lint -> lint item
  - When check_types is specific, only that section's items are required
  - When check_types=all, all items required (current behavior)

"## Deep Inspection Protocol":
- Only triggered when evidence_bar=high or evidence_bar=auditor

Do NOT change YAML frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" skills/verification-before-completion/SKILL.md | grep -q "1" && grep -q "evidence_bar" skills/verification-before-completion/SKILL.md && grep -q "auto_run" skills/verification-before-completion/SKILL.md && grep -q "check_types" skills/verification-before-completion/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- skills/verification-before-completion/SKILL.md contains "## Parameters (caller controls)" section
- Parameter table has evidence_bar (low/standard/high/auditor), auto_run (true/false), check_types (tests/build/lint/types/all)
- Gate section adapts to check_types and auto_run
- Evidence table adapts to evidence_bar
- Verification Checklist adapts to check_types
- Default behavior (standard, true, all) preserves current behavior
  </acceptance_criteria>
  <done>verification-before-completion SKILL.md has parameter table with evidence_bar/auto_run/check_types, body sections reference parameters, defaults unchanged</done>
</task>

</tasks>

<verification>
Both skill files contain a "Parameters (caller controls)" table. Parameters are referenced in body sections. Default behavior is backward compatible.
</verification>

<success_criteria>
- skills/test-driven-development/SKILL.md has parameter table and parameter-aware sections
- skills/verification-before-completion/SKILL.md has parameter table and parameter-aware sections
- grep confirms "Parameters (caller controls)" in both files
- No YAML frontmatter changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill-parameterization/05-02-SUMMARY.md`
</output>
