---
phase: 05-skill-parameterization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/adversarial-review/SKILL.md
  - skills/subagent-driven-development/SKILL.md
autonomous: true
requirements:
  - PARAM-09
  - PARAM-10

must_haves:
  truths:
    - "adversarial-review skill accepts intensity, min_findings, and focus parameters"
    - "subagent-driven-development skill accepts worker_model, review_between, and parallelism parameters"
    - "Both skills work identically when no parameters are specified (backward compatible)"
  artifacts:
    - path: "skills/adversarial-review/SKILL.md"
      provides: "Parameterized adversarial review skill"
      contains: "Parameters (caller controls)"
    - path: "skills/subagent-driven-development/SKILL.md"
      provides: "Parameterized subagent-driven development skill"
      contains: "Parameters (caller controls)"
  key_links:
    - from: "skills/adversarial-review/SKILL.md"
      to: "parameter table"
      via: "Review Protocol and severity classification adapt to intensity and focus"
      pattern: "intensity|min_findings|focus"
    - from: "skills/subagent-driven-development/SKILL.md"
      to: "parameter table"
      via: "Process and dispatch sections adapt to worker_model and parallelism"
      pattern: "worker_model|review_between|parallelism"
---

<objective>
Parameterize adversarial-review and subagent-driven-development skills with caller-controlled parameters.

Purpose: Final two skills become tunable canvases -- callers control review intensity, worker model selection, and parallelism strategy via prompt parameters.
Output: Two updated SKILL.md files with parameter tables and parameter-aware body sections.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From agents/multi-perspective-review.md:
- Table: Parameter | Default | Range | Description
- Natural language parsing hints
- Parameters referenced throughout body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parameterize adversarial-review skill</name>
  <files>skills/adversarial-review/SKILL.md</files>
  <read_first>skills/adversarial-review/SKILL.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after "## Overview".

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `intensity` | standard | gentle, standard, hostile | Review aggression level. gentle=blocking issues only (proportional review: light), standard=material issues (proportional review: standard/full), hostile=assume everything is broken, find every flaw |
| `min_findings` | 0 | 0-10 | Minimum findings before accepting review as complete. 0=no floor. When set, zero-finding reviews trigger re-analysis with stronger prompting |
| `focus` | all | all, security, correctness, completeness, performance | Which review categories to prioritize. all=full review protocol, specific=deep-dive that category only |

Add parsing hints: "Parse from caller prompt. 'Be gentle' -> intensity=gentle. 'Tear it apart' -> intensity=hostile. 'Find at least 5 issues' -> min_findings=5. 'Security review only' -> focus=security."

Update sections to reference parameters:

"## Review Protocol":
- Step 3 (Hunt for Issues): When focus != all, prioritize that category. "When focus=security, spend 80% of review time on the Security checklist. Still note blocking issues in other categories, but don't deep-dive them."
- Step 4 (Classify Findings): intensity=gentle -> only report Blocking severity. intensity=standard -> report Blocking + Important (current behavior). intensity=hostile -> report everything including nits, treat Important as Blocking.
- Step 5 (Zero-Issue Check): "When min_findings > 0 and findings < min_findings, re-examine with stronger prompting. This is not about manufacturing issues -- it's about ensuring thoroughness."

"## Proportional review" section: Map intensity to existing proportional review levels:
- gentle = "Light review" row
- standard = "Standard review" + "Full adversarial" rows
- hostile = Always "Full adversarial" regardless of change size

"## The Iron Principle": Unchanged -- always applies regardless of parameters.

"## Multi-Perspective Review (Council Mode)":
- "### Pedanticness Slider" already exists -- note that it maps to intensity parameter: gentle=Low, standard=Medium, hostile=High.

Do NOT change YAML frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" skills/adversarial-review/SKILL.md | grep -q "1" && grep -q "intensity" skills/adversarial-review/SKILL.md && grep -q "min_findings" skills/adversarial-review/SKILL.md && grep -q "focus.*all" skills/adversarial-review/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- skills/adversarial-review/SKILL.md contains "## Parameters (caller controls)" section
- Parameter table has intensity (gentle/standard/hostile), min_findings (0-10), focus (all/security/correctness/completeness/performance)
- Review Protocol steps adapt to intensity and focus
- Zero-Issue Check adapts to min_findings
- Pedanticness Slider maps to intensity
- Default behavior (standard, 0, all) preserves current behavior
  </acceptance_criteria>
  <done>adversarial-review SKILL.md has parameter table with intensity/min_findings/focus, body sections reference parameters, defaults unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize subagent-driven-development skill</name>
  <files>skills/subagent-driven-development/SKILL.md</files>
  <read_first>skills/subagent-driven-development/SKILL.md, agents/multi-perspective-review.md</read_first>
  <action>
Add a "## Parameters (caller controls)" section immediately after the overview paragraph (after "Fresh subagent per task + two-stage review...").

Parameters to add:

| Parameter | Default | Range | Description |
|-----------|---------|-------|-------------|
| `worker_model` | sonnet | haiku, sonnet, opus | Model for implementation subagents. haiku=deterministic tasks with rich context, sonnet=balanced (default), opus=complex architecture work |
| `review_between` | true | true, false | Whether to run spec+quality review between each task. false=skip reviews, run single final review only (faster but riskier) |
| `parallelism` | sequential | sequential, conservative, aggressive | Task dispatch strategy. sequential=one at a time (current), conservative=2 parallel if non-overlapping files, aggressive=all independent tasks parallel |

Add parsing hints: "Parse from caller prompt. 'Use haiku for workers' -> worker_model=haiku. 'Skip intermediate reviews' -> review_between=false. 'Run tasks in parallel' -> parallelism=aggressive."

Update sections to reference parameters:

"## The Process" (the dot graph):
- When review_between=false: Skip "Dispatch spec reviewer" and "Dispatch code quality reviewer" nodes between tasks. Only run final code reviewer at the end.
- Add note: "When review_between=false, quality gates are deferred to the final review. This is faster but risks compounding issues across tasks."

"## The Process" dispatch steps:
- worker_model: "Dispatch implementer subagent using {worker_model} model." Add: "Override per-task if the task's complexity warrants it (e.g., a haiku default can still use opus for an architecture task)."
- parallelism=sequential: Current behavior (one at a time)
- parallelism=conservative: "Analyze task file domains. Dispatch up to 2 tasks simultaneously if their file domains don't overlap. Wait for both to complete before dispatching next batch."
- parallelism=aggressive: "Dispatch all tasks with non-overlapping file domains simultaneously. Monitor for conflicts. If file conflict detected, serialize the conflicting tasks."

"## Watch For":
- Add: "When parallelism != sequential, file domain analysis is critical. Overlapping file domains MUST be serialized regardless of parallelism setting."

"## Advantages" / "## Cost":
- Add parallelism cost note: "aggressive parallelism uses more concurrent context but completes faster. Monitor total context consumption."

Do NOT change YAML frontmatter. Keep backward compatibility.
  </action>
  <verify>
    <automated>grep -c "Parameters (caller controls)" skills/subagent-driven-development/SKILL.md | grep -q "1" && grep -q "worker_model" skills/subagent-driven-development/SKILL.md && grep -q "review_between" skills/subagent-driven-development/SKILL.md && grep -q "parallelism" skills/subagent-driven-development/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- skills/subagent-driven-development/SKILL.md contains "## Parameters (caller controls)" section
- Parameter table has worker_model (haiku/sonnet/opus), review_between (true/false), parallelism (sequential/conservative/aggressive)
- Process sections reference parameters with conditional behavior
- Watch For section addresses parallelism concerns
- Default behavior (sonnet, true, sequential) preserves current behavior
  </acceptance_criteria>
  <done>subagent-driven-development SKILL.md has parameter table with worker_model/review_between/parallelism, body sections reference parameters, defaults unchanged</done>
</task>

</tasks>

<verification>
Both skill files contain a "Parameters (caller controls)" table. Parameters are referenced in body sections. Default behavior is backward compatible.
</verification>

<success_criteria>
- skills/adversarial-review/SKILL.md has parameter table and parameter-aware sections
- skills/subagent-driven-development/SKILL.md has parameter table and parameter-aware sections
- grep confirms "Parameters (caller controls)" in both files
- No YAML frontmatter changes
</success_criteria>

<output>
After completion, create `.planning/phases/05-skill-parameterization/05-03-SUMMARY.md`
</output>
