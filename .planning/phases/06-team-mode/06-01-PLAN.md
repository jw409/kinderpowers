---
phase: 06-team-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gsd/workflows/map-codebase.md
  - gsd/workflows/execute-phase.md
  - gsd/workflows/new-project.md
autonomous: true
requirements: [TEAM-01, TEAM-03, TEAM-04]

must_haves:
  truths:
    - "map-codebase spawns 4 named mapper agents via TeamCreate when available"
    - "execute-phase spawns executor agents via TeamCreate per wave when available"
    - "new-project spawns 4 researchers + synthesizer via TeamCreate when available"
    - "All three workflows fall back to Task(background) when TeamCreate unavailable"
    - "All three workflows call TeamDelete after work completes"
  artifacts:
    - path: "gsd/workflows/map-codebase.md"
      provides: "TeamCreate gsd-mapping team with 4 named mapper agents"
      contains: "TeamCreate"
    - path: "gsd/workflows/execute-phase.md"
      provides: "TeamCreate gsd-phase-N team with executor agents per wave"
      contains: "TeamCreate"
    - path: "gsd/workflows/new-project.md"
      provides: "TeamCreate gsd-research team with 4 researchers + synthesizer"
      contains: "TeamCreate"
  key_links:
    - from: "gsd/workflows/map-codebase.md"
      to: "TeamCreate"
      via: "detect_runtime_capabilities step checks for TeamCreate tool"
      pattern: "TeamCreate|TeamDelete"
    - from: "gsd/workflows/execute-phase.md"
      to: "TeamCreate"
      via: "execute_waves step uses TeamCreate per wave"
      pattern: "TeamCreate|TeamDelete"
    - from: "gsd/workflows/new-project.md"
      to: "TeamCreate"
      via: "research step uses TeamCreate for parallel researchers"
      pattern: "TeamCreate|TeamDelete"
---

<objective>
Update 3 GSD workflow files to use TeamCreate + named Agent spawns instead of fire-and-forget Task(background) calls. Each workflow gets fallback detection (TeamCreate available?), team lifecycle management (create -> work -> shutdown -> delete), and graceful degradation to Task(background) for older Claude Code versions.

Purpose: Enable inter-agent communication via SendMessage during multi-agent workflows, replacing fire-and-forget Task spawns that can't share discoveries.
Output: 3 updated workflow .md files with TeamCreate/Agent/SendMessage/TeamDelete patterns.
</objective>

<execution_context>
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/workflows/execute-plan.md
@/home/jw/.claude/plugins/cache/kinderpowers/kinderpowers/6.0.0/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TeamCreate to map-codebase.md and new-project.md</name>
  <files>gsd/workflows/map-codebase.md, gsd/workflows/new-project.md</files>
  <read_first>
    - gsd/workflows/map-codebase.md (current workflow -- understand detect_runtime_capabilities and spawn_agents steps)
    - gsd/workflows/new-project.md (current workflow -- understand Step 6 research spawn pattern)
  </read_first>
  <action>
**map-codebase.md changes:**

1. In `detect_runtime_capabilities` step, add TeamCreate detection BEFORE the existing Task detection:

```markdown
**How to detect (priority order):**

1. Check if you have access to a `TeamCreate` tool. If yes → use `team_spawn` path in spawn_agents.
2. Check if you have access to a `Task` tool. If yes → use `task_spawn` path in spawn_agents.
3. Neither available → go to `sequential_mapping`.
```

2. In `spawn_agents` step, add a `team_spawn` condition block BEFORE the existing `Task tool is available` block. The team_spawn block:

```markdown
<step name="spawn_agents" condition="TeamCreate tool is available">
TeamCreate({team_name: "gsd-mapping", description: "Parallel codebase mapping — 4 focus areas"})

Spawn 4 named mapper agents using Agent (NOT Task):

**Agent 1: Tech Focus**
Agent({
  name: "mapper-tech",
  team_name: "gsd-mapping",
  subagent_type: "gsd-codebase-mapper",
  model: "{mapper_model}",
  run_in_background: true,
  prompt: "Focus: tech\n\nAnalyze this codebase for technology stack and external integrations.\n\nWrite these documents to .planning/codebase/:\n- STACK.md - Languages, runtime, frameworks, dependencies, configuration\n- INTEGRATIONS.md - External APIs, databases, auth providers, webhooks\n\nExplore thoroughly. Write documents directly using templates.\nWhen done, SendMessage({to: '*', message: 'Tech mapping complete', summary: 'Wrote STACK.md and INTEGRATIONS.md'}).\nReturn confirmation only."
})

**Agent 2: Architecture Focus**
Agent({
  name: "mapper-arch",
  team_name: "gsd-mapping",
  subagent_type: "gsd-codebase-mapper",
  model: "{mapper_model}",
  run_in_background: true,
  prompt: "Focus: arch\n\nAnalyze this codebase architecture and directory structure.\n\nWrite these documents to .planning/codebase/:\n- ARCHITECTURE.md - Pattern, layers, data flow, abstractions, entry points\n- STRUCTURE.md - Directory layout, key locations, naming conventions\n\nExplore thoroughly. Write documents directly using templates.\nWhen done, SendMessage({to: '*', message: 'Architecture mapping complete', summary: 'Wrote ARCHITECTURE.md and STRUCTURE.md'}).\nReturn confirmation only."
})

**Agent 3: Quality Focus**
Agent({
  name: "mapper-quality",
  team_name: "gsd-mapping",
  subagent_type: "gsd-codebase-mapper",
  model: "{mapper_model}",
  run_in_background: true,
  prompt: "Focus: quality\n\nAnalyze this codebase for coding conventions and testing patterns.\n\nWrite these documents to .planning/codebase/:\n- CONVENTIONS.md - Code style, naming, patterns, error handling\n- TESTING.md - Framework, structure, mocking, coverage\n\nExplore thoroughly. Write documents directly using templates.\nWhen done, SendMessage({to: '*', message: 'Quality mapping complete', summary: 'Wrote CONVENTIONS.md and TESTING.md'}).\nReturn confirmation only."
})

**Agent 4: Concerns Focus**
Agent({
  name: "mapper-concerns",
  team_name: "gsd-mapping",
  subagent_type: "gsd-codebase-mapper",
  model: "{mapper_model}",
  run_in_background: true,
  prompt: "Focus: concerns\n\nAnalyze this codebase for technical debt, known issues, and areas of concern.\n\nWrite this document to .planning/codebase/:\n- CONCERNS.md - Tech debt, bugs, security, performance, fragile areas\n\nIf you receive a message about layer violations from mapper-arch, incorporate those findings.\nExplore thoroughly. Write document directly using template.\nWhen done, SendMessage({to: '*', message: 'Concerns mapping complete', summary: 'Wrote CONCERNS.md'}).\nReturn confirmation only."
})

Wait for all 4 agents to complete (idle notifications).

**Shutdown team:**
TeamDelete({team_name: "gsd-mapping"})

Continue to verify_output.
</step>
```

3. Rename the existing `spawn_agents` step to have condition `Task tool is available but TeamCreate is NOT`. Keep its content unchanged (the existing Task-based spawns). This is the fallback path.

**new-project.md changes:**

4. In Step 6 (Research Decision), wrap the 4 `Task(...)` researcher spawns in a team-aware block:

BEFORE the 4 Task spawns, add detection:

```markdown
**Detect team capability:**

Check if `TeamCreate` tool is available.

**If TeamCreate available:**

TeamCreate({team_name: "gsd-research", description: "Parallel domain research — 4 dimensions"})

Then replace each `Task(prompt=..., subagent_type="gsd-researcher", ...)` with:

Agent({
  name: "researcher-stack",
  team_name: "gsd-research",
  subagent_type: "gsd-researcher",
  model: "{researcher_model}",
  run_in_background: true,
  prompt: [existing prompt + "\nWhen done, SendMessage({to: '*', message: 'Stack research complete', summary: '[key finding]'})."]
})

(Similarly for researcher-features, researcher-arch, researcher-pitfalls)

After all 4 complete, spawn synthesizer in the same team:

Agent({
  name: "synthesizer",
  team_name: "gsd-research",
  subagent_type: "gsd-researcher",
  model: "{synthesizer_model}",
  run_in_background: false,
  prompt: [existing synthesizer prompt]
})

After synthesizer completes:
TeamDelete({team_name: "gsd-research"})

**If TeamCreate NOT available (fallback):**

Use existing Task(...) spawns unchanged.
```

5. Do NOT change plan-phase.md -- it spawns single agents (researcher, planner, checker) and does not benefit from team overhead. Note this explicitly in a comment if helpful.
  </action>
  <verify>
    <automated>grep -c "TeamCreate" gsd/workflows/map-codebase.md && grep -c "TeamDelete" gsd/workflows/map-codebase.md && grep -c "TeamCreate" gsd/workflows/new-project.md && grep -c "TeamDelete" gsd/workflows/new-project.md && grep -c "Agent(" gsd/workflows/map-codebase.md && grep -c "Agent(" gsd/workflows/new-project.md</automated>
  </verify>
  <acceptance_criteria>
    - map-codebase.md contains "TeamCreate" at least once
    - map-codebase.md contains "TeamDelete" at least once
    - map-codebase.md contains "Agent(" at least 4 times (4 mapper agents)
    - map-codebase.md contains "team_name" in Agent calls
    - map-codebase.md retains Task fallback path with condition "Task tool is available but TeamCreate is NOT"
    - map-codebase.md retains sequential_mapping fallback
    - new-project.md contains "TeamCreate" at least once
    - new-project.md contains "TeamDelete" at least once
    - new-project.md contains "Agent(" at least 5 times (4 researchers + 1 synthesizer)
    - new-project.md retains Task fallback for when TeamCreate unavailable
    - plan-phase.md is NOT modified (single-agent workflows stay as Task)
  </acceptance_criteria>
  <done>map-codebase.md and new-project.md use TeamCreate/Agent/TeamDelete when available, fall back to Task(background) when not, and retain sequential fallback for runtimes without either.</done>
</task>

<task type="auto">
  <name>Task 2: Add TeamCreate to execute-phase.md</name>
  <files>gsd/workflows/execute-phase.md</files>
  <read_first>
    - gsd/workflows/execute-phase.md (current workflow -- understand execute_waves step and how agents are spawned per wave)
  </read_first>
  <action>
**execute-phase.md changes:**

1. In the `execute_waves` step, section 2 ("Spawn executor agents"), wrap in team-aware block:

BEFORE spawning agents for each wave, add team detection and lifecycle:

```markdown
**Detect team capability (once per phase, before first wave):**

```bash
# Check if TeamCreate is available by attempting to reference the tool
TEAM_CAPABLE=false  # Set to true if TeamCreate tool exists in your runtime
```

**If TEAM_CAPABLE:**

**For each wave:**

a. Create wave team:
TeamCreate({team_name: "gsd-phase-{PHASE_NUMBER}-wave-{WAVE}", description: "Phase {PHASE_NUMBER} wave {WAVE} executors"})

b. Spawn executors as named agents:
For each plan in wave:
Agent({
  name: "executor-{plan_id}",
  team_name: "gsd-phase-{PHASE_NUMBER}-wave-{WAVE}",
  subagent_type: "gsd-executor",
  model: "{executor_model}",
  run_in_background: true,
  prompt: [existing executor prompt unchanged + "\n\nIf you discover unexpected patterns or deviations that other executors in this wave should know about, use SendMessage({to: '*', message: '[finding]', summary: '[brief]'})."]
})

c. Wait for all agents in wave to complete (idle notifications).

d. Delete wave team:
TeamDelete({team_name: "gsd-phase-{PHASE_NUMBER}-wave-{WAVE}"})

e. Continue to spot-check, pre-wave dependency check, next wave.

**If NOT TEAM_CAPABLE (fallback):**

Use existing Task(...) spawn pattern unchanged.
```

2. In the `verify_phase_goal` step, the verifier is already spawned as a single Task -- leave it as-is (same rationale as plan-phase: no team overhead for 1 agent).

3. Add to `runtime_compatibility` section at the top:

```markdown
**Team mode (v2.1.77+):**
- When `TeamCreate` tool is available, each wave creates a team for inter-agent communication
- Executors can share patterns, deviations, and discoveries via `SendMessage`
- Team is deleted after each wave completes
- Falls back to fire-and-forget `Task` spawns if `TeamCreate` is unavailable
```
  </action>
  <verify>
    <automated>grep -c "TeamCreate" gsd/workflows/execute-phase.md && grep -c "TeamDelete" gsd/workflows/execute-phase.md && grep -c "Agent(" gsd/workflows/execute-phase.md && grep "Team mode" gsd/workflows/execute-phase.md | head -1</automated>
  </verify>
  <acceptance_criteria>
    - execute-phase.md contains "TeamCreate" at least once
    - execute-phase.md contains "TeamDelete" at least once
    - execute-phase.md contains "Agent(" for executor spawns
    - execute-phase.md contains "Team mode" in runtime_compatibility section
    - execute-phase.md retains Task fallback for when TeamCreate unavailable
    - execute-phase.md verifier remains as single Task (not converted to team)
    - execute-phase.md team is per-wave (created and deleted per wave, not per phase)
  </acceptance_criteria>
  <done>execute-phase.md creates per-wave teams with named executor agents when TeamCreate available, falls back to Task spawns otherwise. Verifier remains single Task. Team lifecycle is wave-scoped.</done>
</task>

</tasks>

<verification>
All 3 multi-agent workflow files updated:
- `grep -l "TeamCreate" gsd/workflows/*.md` returns map-codebase.md, execute-phase.md, new-project.md
- `grep -l "TeamDelete" gsd/workflows/*.md` returns map-codebase.md, execute-phase.md, new-project.md
- `grep -l "Task(" gsd/workflows/map-codebase.md gsd/workflows/execute-phase.md gsd/workflows/new-project.md` still returns all 3 (fallback paths retained)
- `grep -L "TeamCreate" gsd/workflows/plan-phase.md` returns plan-phase.md (confirming it was NOT modified)
</verification>

<success_criteria>
- 3 workflow files use TeamCreate + Agent when available
- 3 workflow files fall back to Task(background) when TeamCreate unavailable
- 3 workflow files call TeamDelete after team work completes
- plan-phase.md unchanged (single-agent workflow, no team overhead)
- map-codebase.md still has sequential_mapping fallback for no-Task runtimes
</success_criteria>

<output>
After completion, create `.planning/phases/06-team-mode/06-01-SUMMARY.md`
</output>
